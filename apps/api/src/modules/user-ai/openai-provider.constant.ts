/** UserConnection.provider value for per-user OpenAI API keys */
export const OPENAI_USER_CONNECTION_PROVIDER = 'openai';

/** Metadata: { fromE164: string } — number you use as "From" in the phone simulator as the agent */
export const SIMULATOR_AGENT_CONNECTION_PROVIDER = 'simulator_agent';
